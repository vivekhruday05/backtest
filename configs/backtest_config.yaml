# Backtesting Simulation Configuration

# Paths
data_dir: "data"
output_dir: "output"

# Simulation parameters
simulation:
  start_date: "20221101"
  end_date: "20221130"
  initial_capital: 1000000.0  # 1,000,000 INR
  slippage_pct: 0.0005        # 0.05% execution slippage (approx 5 bps)
  transaction_fee_pct: 0.0002 # 0.02% brokerage, taxes, exchange charges

# Strategy Configuration
strategy:
  xml_path: "strategies/atm_straddle_strategy.xml"
